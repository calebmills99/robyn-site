# Components

This Astro site currently has **no shared UI primitive library** under `src/components/` (directory exists empty / unused in sync). Styling is custom CSS in `src/styles/global.css` with semantic classes (`.btn`, `.hero`, `.section`, etc.).

When designing, reproduce via those CSS classes and BaseLayout, not a component kit.
