{
  "name": "robyn-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "ingest": "node scripts/ingest-content.mjs",
    "prebuild": "node scripts/ingest-content.mjs",
    "dev": "astro dev",
    "build": "astro build && node -e \"import fs from 'fs'; fs.copyFileSync('dist/sitemap-index.xml','dist/sitemap.xml');\"",
    "preview": "astro preview",
    "astro": "astro",
    "deploy:staging": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "wrangler": "^4.113.0"
  }
}
