{
  "name": "robyn-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "ingest": "node scripts/ingest-content.mjs",
    "prebuild": "node scripts/ingest-content.mjs && node scripts/mirror-blog-cdn.cjs && node scripts/mirror-wp-uploads.cjs",
    "dev": "astro dev",
    "build": "astro build && node -e \"import fs from 'fs'; const source = fs.existsSync('dist/sitemap-index.xml') ? 'dist/sitemap-index.xml' : 'dist/sitemap-0.xml'; fs.copyFileSync(source, 'dist/sitemap.xml');\"",
    "preview": "astro preview",
    "astro": "astro",
    "deploy:staging": "npm run build && wrangler deploy",
    "mirror:blog": "node scripts/mirror-blog-cdn.cjs",
    "mirror:wp": "node scripts/mirror-wp-uploads.cjs"
  },
  "dependencies": {
    "@astrojs/markdown-remark": "^7.2.1",
    "@astrojs/sitemap": "^3.7.0",
    "astro": "^7.1.3",
    "mdast-util-from-markdown": "^2.0.3",
    "unist-util-visit": "^5.1.0"
  },
  "devDependencies": {
    "gray-matter": "^4.0.3",
    "wrangler": "^4.113.0"
  }
}
